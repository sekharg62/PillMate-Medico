[build]
  command = "npm run build"
  publish = "dist"

# React Router (SPA): avoid 404 on refresh or direct links to /dashboard, /login, etc.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
